wifi:
  ssid: "IoT"
  password: !secret wifi_password
  domain: .lan
  power_save_mode: NONE
  reboot_timeout: 120s

logger:
  baud_rate: 0

api:

ota:
  - platform: esphome
  - platform: web_server

web_server:
  port: 80
  local: true

button:
  - platform: restart
    id: reboot
    name: Reboot

external_components:
  - source: components
