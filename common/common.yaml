wifi:
  ssid: "IoT"
  password: !secret wifi_password
  domain: .lan
  reboot_timeout: 120s

logger:
  baud_rate: 0

api:

ota:

web_server:
  port: 80
  local: true

button:
  - platform: restart
    id: reboot
    name: Reboot

external_components:
  - source: components